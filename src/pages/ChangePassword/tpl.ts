const tpl: string = `
{{{backButton}}}

<div class="change_password">
    <form>
        <div class="change_password__inputs">
            {{{inputs}}}
        </div>

        <div class="change_password__buttons">
            {{{saveButton}}}
        </div>
    </form>
</div>
`;

export default tpl;
